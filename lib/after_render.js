/*
 * Dependencies.
 */


/*
 * After render module. Each method defined here gets
 * executed after view rendering.
 */
var afterRender = {
    // yourFunction: function(req, res) {}
};

module.exports = afterRender;
